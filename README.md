# Experiment-Dotnet-HelloWorld

Simple example .NET console hello world application.

## Continuous integration

A GitHub Actions workflow is included at `.github/workflows/dotnet.yml`.
It restores, builds, and runs the application on pushes to `main` and on pull requests.
